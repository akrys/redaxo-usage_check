
<ul>
	<li>Bugfix: Wenn es keine XFORM-Tabelle gibt, funktioniert die Abfrage nach ungenutzten Dateien nicht.</li>
	<li>Link zum Quellcode auch generieren, wenn ein Element nicht genutzt wird</li>
	<li>User-Rechte beim Seiten-Auruf berücksichtigen</li>
	<li>Generierung der ISO-Kodierten-Sprachdateien für das Redaxo Backend (Wenn benötigt &amp; braucht schreibrechte am <code>lang</code>-Ordner)</li>
	<li>Encoding Bugfix. Durch ein <code>concat</code> von <code>integer</code> und <code>varchar</code> Spalten ergeben sich Encodingprobleme in MySQL &lt; 5.5<br /></li>
</ul>

