
<ul>
	<li>Namespace adjustment to the FOR standard. (<code>FriendsOfRedaxo\UsageCheck</code> instead of <code>FriendsOfRedaxo\addon\UsageCheck</code>)</li>
	<li>
		Autoloader marked as deprecated.<br />
		<small>
			As all classes are moved to <code>lib/</code>, it is not necessary anymore. wird er nicht mehr benötigt.<br />
			Until the release of v4 classes from the old namespace will be redirected to the new one using <code>class_alias</code>.
		</small>
	</li>
	<li>text updates in <code>readme.md</code> and <code>help.md</code>.</li>
</ul>
